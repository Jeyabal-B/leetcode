class Solution {
    public boolean containsDuplicate(int[] nums) {
        Map map = new HashMap<Integer,Integer>();
        for(int i: nums){
            map.put(i,0);
        }
        if(nums.length==map.size()){
            return false;
        }else{
            return true;
        }
    }
}
